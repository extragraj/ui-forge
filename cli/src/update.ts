// Deprecated — `ui-forge update` now redirects to `ui-forge repair`.
// The deprecation shim lives in index.ts. Kept for import compatibility only.
export { runRepair as runUpdate } from './repair.js';
