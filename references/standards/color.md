# Color Standard

<!--
  TEMPLATE — intentionally empty.

  This file is a skill-owned FALLBACK. While it contains only headings and
  comments, UI Forge treats it as a placeholder and skips it — no content is
  injected into generation context.

  Fill this in to ship opinionated color defaults to every project using
  this skill. A project-local override at PROJECT_ROOT/design/standards/
  color.md takes precedence; an explicit entry in design-arch.json's
  designStandards takes precedence over both.

  Suggested sections (uncomment / fill as needed):
    - Semantic tokens allowed in JSX (primary, secondary, accent, muted, ...)
    - Raw hex policy — typically: never in JSX, only in tailwind.config
    - Contrast floors (WCAG AA for body, AAA aspirational for long prose)
    - Dark-mode parity rules (every surface must resolve in both modes)
    - Status colors (success, warning, destructive) and where they apply
    - Gradient policy (permitted combinations, forbidden combinations)
    - Brand accents — when to reach for them, when NOT to
    - Hover / focus / active state tokens (usually -muted or opacity layer)

  Keep the whole file under ~3,000 characters — the injector truncates past that.
-->

## Semantic tokens

## Contrast

## Dark mode

## Gradient policy
