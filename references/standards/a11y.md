# Accessibility Standard

<!--
  TEMPLATE — intentionally empty.

  This file is a skill-owned FALLBACK. While it contains only headings and
  comments, UI Forge treats it as a placeholder and skips it — no content is
  injected into generation context.

  If the +A11Y modifier is active (via --a11y flag, arch.a11yRequired, or the
  StackShift paired-mode marker), UI Forge already injects the SIGNAL_A11Y
  instruction block from references/prompt-patterns.md. This file is for
  ADDITIONAL, project-specific a11y guidance that complements SIGNAL_A11Y.

  Anything put here must be compatible with SIGNAL_A11Y — do not contradict
  WCAG 2.1 AA rules already enforced there. Use this slot for nuances unique
  to the target project (e.g. required aria-live regions, a tested contrast
  matrix for brand tokens, keyboard shortcut conventions).

  Suggested sections (uncomment / fill as needed):
    - Landmark usage patterns for this project (main, nav, aside allocation)
    - Heading outline conventions per page type
    - Project-specific aria-label copy policies
    - Form field labeling (visible vs aria-label vs aria-labelledby)
    - Keyboard interaction contracts (roving tabindex, escape-to-dismiss, ...)
    - Motion reduction — which animations to strip, which to tame
    - Screen-reader-only text conventions (class name, length guidance)
    - Verified contrast matrix for brand tokens, if known
    - Live-region rules for toasts, loading states, async updates

  Keep the whole file under ~3,000 characters — the injector truncates past that.
-->

## Landmarks

## Labels & names

## Keyboard

## Motion

## Live regions
