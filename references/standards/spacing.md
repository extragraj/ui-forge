# Spacing Standard

<!--
  TEMPLATE — intentionally empty.

  This file is a skill-owned FALLBACK. While it contains only headings and
  comments, UI Forge treats it as a placeholder and skips it — no content is
  injected into generation context.

  Fill this in to ship opinionated spacing defaults to every project using
  this skill. A project-local override at PROJECT_ROOT/design/standards/
  spacing.md takes precedence; an explicit entry in design-arch.json's
  designStandards takes precedence over both.

  Suggested sections (uncomment / fill as needed):
    - Container widths (sm / md / lg / xl / 2xl breakpoint policy)
    - Section vertical rhythm (py-* defaults for hero, content, dense)
    - Gap scale (when to use gap-2 vs gap-4 vs gap-8)
    - Card / element inner padding conventions
    - Max-width for prose vs app-shell regions
    - Responsive overrides (mobile vs desktop padding ratios)
    - Whitespace as visual grouping: when to expand, when to compress
    - "Tight vs airy" section rules — which sections get which

  Keep the whole file under ~3,000 characters — the injector truncates past that.
-->

## Containers

## Section rhythm

## Gap scale

## Responsive overrides
