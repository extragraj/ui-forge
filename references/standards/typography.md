# Typography Standard

<!--
  TEMPLATE — intentionally empty.

  This file is a skill-owned FALLBACK. While it contains only headings and
  comments, UI Forge treats it as a placeholder and skips it — no content is
  injected into generation context.

  Fill this in to ship opinionated typography defaults to every project using
  this skill. A project-local override at PROJECT_ROOT/design/standards/
  typography.md takes precedence; an explicit entry in design-arch.json's
  designStandards takes precedence over both.

  Suggested sections (uncomment / fill as needed):
    - Heading scale (h1–h6 sizes, weights)
    - Body copy (base size, line-height, max-width for readability)
    - Letter spacing / tracking rules
    - Font families (sans / serif / mono allocations)
    - Fluid type recipe (clamp() formula or breakpoint table)
    - Truncation / overflow policy
    - When to use display vs body fonts
    - Mobile overrides, if any

  Keep the whole file under ~3,000 characters — the injector truncates past that.
-->

## Heading scale

## Body copy

## Fluid type

## Families
